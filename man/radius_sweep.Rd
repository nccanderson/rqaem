% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radius-sweep.R
\name{radius_sweep}
\alias{radius_sweep}
\title{Sweep RQA metrics across a grid of candidate radii}
\usage{
radius_sweep(
  fixations,
  radii,
  line_length = 2L,
  min_cluster = 8L,
  duration = NULL,
  bootstrap_n = 0L,
  bootstrap_type = c("shuffle", "uniform"),
  screen = NULL,
  metrics = c("rec", "det", "lam", "corm"),
  seed = NULL,
  level = 0.95
)
}
\arguments{
\item{fixations}{Numeric matrix / data.frame with two columns
(\code{x}, \code{y}), or a numeric / integer vector of categorical codes.}

\item{radii}{Numeric vector of candidate radii to sweep across.}

\item{line_length, min_cluster}{Forwarded to \code{\link[=rqa]{rqa()}}.}

\item{duration}{Optional duration vector forwarded to \code{\link[=rqa]{rqa()}}.}

\item{bootstrap_n}{If \verb{> 0}, runs \code{\link[=rqa_bootstrap]{rqa_bootstrap()}} with this many
resamples at each radius and attaches \code{boot_mean} / \code{boot_lo} /
\code{boot_hi} columns. Defaults to \code{0} (no bootstrap).}

\item{bootstrap_type}{Forwarded to \code{\link[=rqa_bootstrap]{rqa_bootstrap()}} when
\code{bootstrap_n > 0}. Defaults to \code{"shuffle"}.}

\item{screen}{Required when \code{bootstrap_type = "uniform"}.}

\item{metrics}{Character vector of scalar RQA metrics to record.
Defaults to the four most commonly swept (\code{rec}, \code{det}, \code{lam},
\code{corm}).}

\item{seed}{Optional integer seed; affects the bootstrap path only.
When supplied, the global RNG state is restored on exit so the
sweep is idempotent across calls.}

\item{level}{Confidence level for the percentile interval when a
bootstrap is run. Defaults to \code{0.95}.}
}
\value{
A tibble of class \code{radius_sweep} with one row per
(radius, metric) pair: \code{radius}, \code{metric}, \code{value}. When
\code{bootstrap_n > 0}, three more columns are appended:
\code{boot_mean}, \code{boot_lo}, \code{boot_hi}.
}
\description{
Runs \code{\link[=rqa]{rqa()}} at every radius in \code{radii} and returns a long tibble
with one row per (radius, metric) pair. Optionally runs a
\code{\link[=rqa_bootstrap]{rqa_bootstrap()}} at each radius to attach a null-baseline column
(mean) and a percentile-bootstrap interval (\code{boot_lo}, \code{boot_hi})
alongside the observed value.
}
\details{
Reproduces the input for Fig. 7 of Anderson et al. (2013), which
plots \code{rec} against radius with a bootstrap ribbon as a way to
choose a working radius.
}
\examples{
set.seed(1)
fix <- cbind(runif(40, 0, 800), runif(40, 0, 600))
radius_sweep(fix, radii = seq(40, 200, by = 40))

}
