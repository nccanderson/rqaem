% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rqa_by.R
\name{rqa_by}
\alias{rqa_by}
\title{Run RQA on each group of a data frame}
\usage{
rqa_by(
  data,
  x = NULL,
  y = NULL,
  category = NULL,
  duration = NULL,
  by,
  radius,
  line_length = 2L,
  min_cluster = 8L
)
}
\arguments{
\item{data}{A data frame.}

\item{x, y}{Tidy-eval column references for the xy coordinates.
Mutually exclusive with \code{category}.}

\item{category}{Tidy-eval column reference for a categorical
sequence (e.g. region-of-interest codes). Mutually exclusive with
\code{x} / \code{y}.}

\item{duration}{Optional tidy-eval column reference for fixation
durations. When non-\code{NULL}, the duration-weighted variant of RQA
is used.}

\item{by}{Character vector of column names to group by.}

\item{radius}{Threshold radius for the recurrence matrix.}

\item{line_length}{Minimum line length for diagonal / vertical line
measures. Defaults to \code{2L}.}

\item{min_cluster}{Minimum component size (in cells) for the
\code{clusters} metric. Defaults to \code{8L}.}
}
\value{
A tibble: the grouping columns followed by the 13 scalar
metrics (\code{n}, \code{nrec}, \code{rec}, \code{det}, \code{revdet}, \code{meanline},
\code{maxline}, \code{ent}, \code{relent}, \code{lam}, \code{tt}, \code{corm}, \code{clusters}),
one row per group.
}
\description{
Tidy-eval entry point for the common case of a long-format fixation
data frame with one row per fixation and one or more grouping
columns (trial, participant, image, ...). Returns a single tibble
with one row per group and the scalar RQA metrics as columns. The
recurrence matrices themselves are not materialised in the output —
call \code{\link[=rqa]{rqa()}} on a single group when you need the matrix.
}
\details{
Supply either \code{x} and \code{y} (for xy fixation coordinates) \strong{or}
\code{category} (for categorical RQA), but not both.
}
\examples{
set.seed(1)
eyedat <- data.frame(
  trial = rep(1:3, each = 20),
  x     = runif(60, 0, 1000),
  y     = runif(60, 0, 1000)
)
rqa_by(eyedat, x = x, y = y, by = "trial", radius = 100)

}
