% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{rqa_bootstrap}
\alias{rqa_bootstrap}
\title{Bootstrap null distributions for RQA metrics}
\usage{
rqa_bootstrap(
  fixations,
  radius,
  line_length = 2L,
  min_cluster = 8L,
  duration = NULL,
  n = 999L,
  type = c("shuffle", "uniform"),
  screen = NULL,
  seed = NULL,
  metrics = c("rec", "det", "lam", "tt", "corm", "ent")
)
}
\arguments{
\item{fixations}{Numeric matrix / data.frame with two columns
(\code{x}, \code{y}), or a numeric / integer vector of categorical codes.}

\item{radius}{Threshold radius forwarded to \code{\link[=rqa]{rqa()}}.}

\item{line_length, min_cluster}{Forwarded to \code{\link[=rqa]{rqa()}}.}

\item{duration}{Optional duration vector forwarded to \code{\link[=rqa]{rqa()}}.
Under \code{type = "shuffle"}, durations are permuted alongside the
fixations they were paired with.}

\item{n}{Number of resamples. Defaults to \code{999}.}

\item{type}{One of \code{"shuffle"} (default) or \code{"uniform"}.}

\item{screen}{Numeric vector \code{c(width, height)} defining the
sampling region for \code{type = "uniform"}. Required for that type
and ignored otherwise.}

\item{seed}{Optional integer seed. When supplied, the resamples are
reproducible across machines and R sessions; the global RNG state
is restored on exit.}

\item{metrics}{Character vector of scalar RQA metrics to record per
resample. Defaults to the six emphasised in the paper.}
}
\value{
A tibble of class \code{rqa_bootstrap}: one row per resample,
one column per requested metric plus a \code{resample} index column.
The empirical (observed) metrics are stored as the \code{"observed"}
attribute (a one-row tibble), and the call parameters as
\code{"params"}.
}
\description{
Generates a null distribution by repeatedly resampling the fixation
sequence and re-running \code{\link[=rqa]{rqa()}} on each resample, then returns the
resulting scalar metrics in long tibble form. Two null types are
supported, matching the strategies used in the paper:
}
\details{
\itemize{
\item \code{type = "shuffle"} permutes the fixation order while keeping the
set of positions (and durations) fixed. Preserves the spatial
distribution; breaks temporal structure.
\item \code{type = "uniform"} draws each resample's fixations independently
from a uniform distribution on \code{screen = c(width, height)}.
Preserves only the screen extents; breaks both spatial clustering
and temporal structure.
}
}
\examples{
set.seed(1)
fix <- cbind(runif(30, 0, 1000), runif(30, 0, 1000))
boot <- rqa_bootstrap(fix, radius = 80, n = 19, seed = 42)
summary(boot)

}
