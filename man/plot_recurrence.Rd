% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_recurrence}
\alias{plot_recurrence}
\title{Recurrence plot from an RQA result or matrix}
\usage{
plot_recurrence(x, ...)
}
\arguments{
\item{x}{An \code{rqa_result} from \code{\link[=rqa]{rqa()}}, or a recurrence matrix
directly (base \code{matrix} or \code{Matrix::dgCMatrix}).}

\item{...}{Ignored (present for S3 compatibility).}
}
\value{
A \code{ggplot} object.
}
\description{
Renders the recurrence matrix as a tile plot with \code{i} decreasing
from top to bottom so the visual layout matches the matrix
representation in the paper. Binary matrices use a single black
fill; duration-weighted matrices use a viridis scale.
}
\examples{
fix <- cbind(c(0, 1, 2, 1, 0), c(0, 1, 2, 1, 0))
plot_recurrence(rqa(fix, radius = 0.6))

}
