% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recurrence-matrix.R
\name{recurrence_matrix}
\alias{recurrence_matrix}
\title{Build a recurrence matrix from a fixation sequence}
\usage{
recurrence_matrix(fixations, radius)
}
\arguments{
\item{fixations}{Numeric matrix / data.frame with two columns (xy),
or a numeric / integer vector of category codes.}

\item{radius}{Threshold distance in the same units as \code{fixations}.}
}
\value{
An \code{n} x \code{n} recurrence matrix with \code{1} where
\code{distance <= radius} and \code{0} elsewhere. The diagonal is always \code{1}
(each fixation recurs with itself). Storage is sparse
(\code{Matrix::dgCMatrix}) when \code{n >= 64}, and a dense base \code{matrix}
below that.
}
\description{
Computes pairwise Euclidean distances and thresholds them at \code{radius}
to produce a binary recurrence matrix. Two fixations recur if they
fall within \code{radius} of each other.
}
\details{
Accepts either:
\itemize{
\item a two-column numeric matrix / data.frame of (x, y) coordinates, or
\item a one-dimensional numeric or integer vector of category codes
(use a sub-unit radius like \code{0.1} so equal codes recur).
}
}
\examples{
fix <- rbind(c(0, 0), c(1, 1), c(100, 100))
recurrence_matrix(fix, radius = 5)

}
