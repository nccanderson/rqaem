% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_fixations}
\alias{plot_fixations}
\title{Plot a fixation sequence with optional radius circles}
\usage{
plot_fixations(fixations, radius = NULL, image = NULL)
}
\arguments{
\item{fixations}{Numeric matrix / data.frame with two columns
(\code{x}, \code{y}).}

\item{radius}{Optional numeric scalar. When supplied, circles of
the given radius are drawn around each fixation.}

\item{image}{Optional file path to a background image. Requires
the \code{magick} package (Suggests).}
}
\value{
A \code{ggplot} object.
}
\description{
Scatter and path plot of a fixation sequence, with optional
circles of radius \code{radius} drawn around each fixation to visualise
the recurrence threshold, and an optional background image.
}
\examples{
fix <- cbind(runif(20, 0, 800), runif(20, 0, 600))
plot_fixations(fix, radius = 80)

}
